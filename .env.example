# OpenAI-compatible gateway or vLLM endpoint.
MODEL_GATEWAY_BASE_URL=
MODEL_GATEWAY_API_KEY=
DEFAULT_MODEL=

# Remote MCP registry. Requests can reference only these IDs.
MCP_SERVERS_JSON=[]
# INTERNAL_MCP_TOKEN=

# Write-tool approval verification. Keep this secret only in trusted backend environments.
# If absent, all approval-required write tools are denied by default.
APPROVAL_SIGNING_SECRET=

# Optional later phases.
OPENLIT_ENDPOINT=
MEM0_API_KEY=
