# Copy to .env.router (gitignored). Never commit real keys.

# Required by LiteLLM proxy auth. Use a strong random value locally/production.
LITELLM_MASTER_KEY=

# Optional until real-provider validation. Set one or both.
GROQ_API_KEY=
OPENROUTER_API_KEY=
